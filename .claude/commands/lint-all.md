Run all linting and type checks for the Empathetic Communication project.

1. **Frontend ESLint**: Run `cd frontend && npm run lint` and report any errors or warnings
2. **CDK TypeScript compile**: Run `cd cdk && npm run build` to check for TypeScript errors
3. **CDK tests**: Run `cd cdk && npm test` to run Jest unit tests

Summarize:
- Total errors and warnings per area
- Specific files and line numbers with issues
- Recommended fixes for any critical errors

If any step fails, show the full error output so I can diagnose it.
