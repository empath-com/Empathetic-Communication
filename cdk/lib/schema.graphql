type TextStreamData {
  sessionId: String!
  data: AWSJSON!
}

type Mutation {
  publishTextStream(sessionId: String!, data: AWSJSON!): TextStreamData @aws_iam
}

type Subscription {
  onTextStream(sessionId: String!): TextStreamData
    @aws_subscribe(mutations: ["publishTextStream"])
}

type Query {
  getTextStream: String
}
